#include "yerel.h"

void
orsi_bildiri_tamami(struct _orst_derleme* Derleme)
{
  /*for(int i = Derleme->bildiriler.hatalar.boyut - 1; i >= 0; i--)
  {
    //orst_uyari* Uyari = Derleme->bildiriler.hatalar.Nesneler[i];
    orsi_bildiri_Uyar(Derleme, Derleme->bildiriler.hatalar.Nesneler[i]);
    free(Uyari);
    orsh_dizi_cikar(Derleme->bildiriler.hatalar);
  }*/
}